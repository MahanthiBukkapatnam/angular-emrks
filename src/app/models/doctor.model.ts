
export interface Doctor {
  dssn:     string;   // maps to D_SSN
  name:     string;   // maps to Name
  startDate:string;   // ISO string from Start_Date
  specialty:string;   // maps to Specialty
  officeName:string;  // maps to Office_Name
}

